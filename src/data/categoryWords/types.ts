export type Category = "pais" | "color" | "fruta" | "animal" | "nombre" | "profesion" | "cosa";

export const CATEGORIES: Category[] = ["pais", "color", "fruta", "animal", "nombre", "profesion", "cosa"];

export type WordsByLetter = Record<string, string[]>;
